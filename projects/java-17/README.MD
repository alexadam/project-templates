# Java Project Template (java 17)

Copy the contents of this folder to yout project's folder:

```sh
rsync -rtv ~/work/project-templates/projects/java-17/ .
```

Then rename classes & packages & pom.xml name + artifactId